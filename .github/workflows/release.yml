# Build the B-SS Go example and publish a GitHub Release (binaries + metrics.json) when a
# vX.Y.Z tag is pushed. PRs run the same build + smoke test without publishing.
#
# Like the Rust sibling, this build has TWO stages: (1) build the native CAS BACnet Stack shared
# library from the stack submodule's C++ source (MSBuild's ReleaseDll|x64 configuration on
# Windows, g++ -shared on Linux - see README.md "Build the native CAS BACnet Stack library"),
# then (2) `go build` this project (CGO_ENABLED=1 - this example is cgo-only) and copy that
# native library next to its output so the vendored cgo adapter can find it at run time.
#
# THIS IS, AS FAR AS THIS PROJECT IS AWARE, THE FIRST AUTOMATED CI COMPILATION GATE THE STACK'S
# GO ADAPTER (CASBACnetStackAdapter.go, vendored+fixed into cas_bacnet_stack/) HAS EVER HAD. It
# shipped broken against the current 6.x header (renamed transport callbacks, a wrong time_t vs
# CASBACnetTime type, only 2 of 6 needed Get*Property callbacks wired) - dead-golang allowlist
# entries #1641/IFC-027 are tracked upstream but not fixed there. See AGENTS.md and
# cas_bacnet_stack/README.md.
#
# The CAS BACnet Stack is a PRIVATE git submodule: the repository secret CAS_STACK_PAT must
# hold a PAT with read access to chipkin/cas-bacnet-stack.
name: build-and-release

on:
  pull_request:
  push:
    tags: ["v*.*.*"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          # windows-2022 pins the VS 2022 (v143) toolset the stack's ReleaseDll config
          # (and the released binaries) are built with. It also ships MinGW-w64/gcc via
          # msys2 is NOT guaranteed, so this workflow installs it explicitly for cgo.
          - os: windows-2022
            artifact: BACnetProfileExampleBSSGo-windows-x64
            exe: bacnet-b-ss-go.exe
            nativelib: CASBACnetStack_x64_Release.dll
            metrics: metrics-windows.json
          - os: ubuntu-latest
            artifact: BACnetProfileExampleBSSGo-linux-x64
            exe: bacnet-b-ss-go
            nativelib: libCASBACnetStack_x64_Release.so
            metrics: metrics-linux.json
    steps:
      - name: Enable git long paths (Windows)
        if: runner.os == 'Windows'
        run: git config --system core.longpaths true

      - name: Checkout (with the CAS BACnet Stack submodule)
        uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.CAS_STACK_PAT || github.token }}

      - name: Stack commit (recorded in metrics.json)
        id: stack
        shell: bash
        run: echo "sha=$(git -C submodules/cas-bacnet-stack rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Set up MinGW-w64 (Windows - the C compiler cgo needs, separate from MSVC)
        if: runner.os == 'Windows'
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64
          install: mingw-w64-x86_64-gcc
          path-type: inherit

      - name: Build the native CAS BACnet Stack library (Windows - MSBuild ReleaseDll|x64)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $msbuild = & "${env:ProgramFiles(x86)}\Microsoft Visual Studio\Installer\vswhere.exe" `
            -latest -products * -requires Microsoft.Component.MSBuild -find "MSBuild\**\Bin\MSBuild.exe" | Select-Object -First 1
          & $msbuild submodules\cas-bacnet-stack\projects\msvs\BuildCASBACnetStack.sln `
            /p:Configuration=ReleaseDll /p:Platform=x64 /m
          if ($LASTEXITCODE -ne 0) { throw "MSBuild failed with exit code $LASTEXITCODE" }
          New-Item -ItemType Directory -Force lib | Out-Null
          Get-ChildItem submodules\cas-bacnet-stack\bin\CASBACnetStack_x64_Release.dll
          Copy-Item submodules\cas-bacnet-stack\bin\CASBACnetStack_x64_Release.dll lib\

      - name: Build the native CAS BACnet Stack library (Linux - g++ shared)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          mkdir -p lib
          g++ -shared -fPIC -std=c++17 -O2 \
              -I submodules/cas-bacnet-stack/source \
              submodules/cas-bacnet-stack/source/*.cpp \
              -o lib/libCASBACnetStack_x64_Release.so
          ls -la lib/libCASBACnetStack_x64_Release.so

      - name: go build (CGO_ENABLED=1)
        shell: bash
        env:
          CGO_ENABLED: "1"
        run: go build -o "${{ matrix.exe }}" .

      - name: Copy the native library next to the Go binary
        shell: bash
        run: cp "lib/${{ matrix.nativelib }}" "${{ matrix.nativelib }}"

      - name: Smoke test + metrics (starts, binds, stays up; measures time to 'ready')
        shell: bash
        run: |
          start=$(date +%s%3N)
          ./"${{ matrix.exe }}" --port 47822 > smoke.log 2>&1 &
          PID=$!
          ready_ms=""
          for i in $(seq 1 100); do
            if grep -q "ready" smoke.log; then ready_ms=$(( $(date +%s%3N) - start )); break; fi
            sleep 0.1
          done
          sleep 2
          if kill -0 "$PID" 2>/dev/null; then kill "$PID" 2>/dev/null || true; else echo "FAIL: device exited early"; cat smoke.log; exit 1; fi
          [ -n "$ready_ms" ] || { echo "FAIL: no 'ready' line"; cat smoke.log; exit 1; }
          cat smoke.log
          bytes=$(stat -c %s "${{ matrix.exe }}" 2>/dev/null || wc -c < "${{ matrix.exe }}")
          sha=$(sha256sum "${{ matrix.exe }}" | cut -c1-16)
          stackver=$(grep -oE 'CAS BACnet Stack v[0-9.]+' smoke.log | head -1 | grep -oE '[0-9.]+' || true)
          appver=$(grep -oE 'v[0-9]+\.[0-9]+\.[0-9]+' smoke.log | head -1 || true)
          cat > "${{ matrix.metrics }}" <<EOF
          {
            "example": "BACnetProfileExampleBSSGo",
            "app_version": "$appver",
            "os": "${{ matrix.os }}",
            "link_mode": "cgo-build-time-link",
            "stack_commit": "${{ steps.stack.outputs.sha }}",
            "stack_version": "$stackver",
            "binary_bytes": $bytes,
            "binary_sha256_prefix": "$sha",
            "startup_ms_to_ready": $ready_ms,
            "go_version": "$(go version)",
            "built_at": "$(date -u +%Y-%m-%dT%H:%M:%SZ)",
            "git_ref": "${{ github.ref_name }}"
          }
          EOF
          cat "${{ matrix.metrics }}"
          { echo '### metrics (${{ matrix.os }})'; echo '```json'; cat "${{ matrix.metrics }}"; echo '```'; } >> "$GITHUB_STEP_SUMMARY"

      - name: Package
        shell: bash
        run: |
          mkdir -p dist
          cp "${{ matrix.exe }}" dist/ 2>/dev/null || true
          cp "${{ matrix.nativelib }}" dist/ 2>/dev/null || true
          cp "${{ matrix.metrics }}" dist/
          cp README.md TUTORIAL.md LICENSE dist/ 2>/dev/null || true
          cp docs/PICS.md dist/ 2>/dev/null || true

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/*

  release:
    name: publish release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
      # Every platform artifact carries the same docs (README.md, LICENSE, ...).
      # Uploading two assets with one name makes the release action fail part-way
      # and leaves a draft release behind (a real tagged release hit this). Flatten the
      # artifacts into one folder with a single copy of each file name, and stop
      # if two files share a name but differ.
      - name: Collect release assets (one copy of each file name)
        shell: bash
        run: |
          mkdir -p release
          find artifacts -type f | sort | while read -r f; do
            name=$(basename "$f")
            if [ -e "release/$name" ]; then
              cmp -s "$f" "release/$name" || { echo "FAIL: two different files are both named $name ($f)"; exit 1; }
            else
              cp "$f" "release/$name"
            fi
          done
          ls -l release
      - uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          generate_release_notes: true
          files: release/*
